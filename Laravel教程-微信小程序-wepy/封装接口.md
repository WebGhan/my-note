## 封装接口请求
文件：src/utils/api.js
```javascript
import wepy from 'wepy'
// 服务器接口地址
const host = 'http://xxx.com/api'
```
#### 普通请求
```javascript
const request = async (options, showLoading = true) => {
  // 显示Loading
  if (showLoading) {
    wepy.showLoading({title: '加载中'})
  }

  // 如果传入的options是url(字符串)则转化成对象
  if (tpyeof options === 'string') {
    options = {
      url: options
    }
  }
  // 拼接请求地址
  options.url = host + '/' + options.url
  // 调用小程序 request 方法
  let response = await wepy.request(options)

  // 隐藏Loading
  if (showLoading) {
    wepy.hideLoading()
  }

  // 服务器异常提示
  if (response.statusCode === 500) {
    wepy.showModal({
      title: '提示',
      content: '服务器错误，请联系管理员或重试'
    })
  }

  return response
}
```
#### 登录
```javascript
const login = async (params = {}) => {
  // 登录凭证(code)只能使用一次，所以每次单独调用
  let loginData = await wepy.login()

  // 参数中增加code
  params.code = loginData.code

  // 请求登录接口 weapp/authorizations
  let authResponse = await request({
    url: 'weapp/authorizations',
    data: params,
    method: 'POST'
  })

  // 登录成功，存储token信息
  if (authResponse.statusCode === 201) {
    wepy.setStorageSync('access_token', authResponse.data.access_token)
    // 过期时间（毫秒数）
    wepy.setStorageSync('access_token_expired_at', new Date().getTime() + authResponse.data.expires_in * 1000)
  }

  return authResponse
}
```
#### 更新 Token
```javascript
const refreshToken = async (accessToken) => {
  // 请求更新接口 authorizations/current
  let refreshResponse = await wepy.request({
    url: host + '/' + 'authorizations/current',
    method: 'PUT',
    header: {
      'Authorization': 'Bearer ' + accessToken
    }
  })

  // 更新成功
  if (refreshResponse.statusCode === 200) {
    // 存入新的 Token
    wepy.setStorageSync('access_token', refreshResponse.data.access_token)
    // 过期时间（毫秒数）
    wepy.setStorageSync('access_token_expired_at', new Date().getTime() + refreshResponse.data.expires_in * 1000)
  }

  return refreshResponse
}
```
#### 获取 Token
```javascript
const getToken = async (options) => {
  // 从 storage 中取出 Token
  let accessToken = wepy.getStorageSync('access_token')
  let expiredAt = wepy.getStorageSync('access_token_expired_at')

  // 如果 Token 过期，则调用更新(refreshToken)方法
  if (accessToken && new Date().getTime() > expiredAt) {
    let refreshResponse = await refreshToken(accessToken)

    // 更新成功
    if (refreshResponse.statusCode === 200) {
      accessToken = refreshResponse.data.access_token
    } else {
      // 更新失败，重新登录
      let authResponse = await login()
      if (authResponse.statusCode === 201) {
        accessToken = authResponse.data.access_token
      }
    }
  }

  return accessToken
}
```
#### 带身份验证的请求
```javascript
const authRequest = async (options, showLoading = true) {
  // 如果传入的options是url(字符串)则转化成对象
  if (tpyeof options === 'string') {
    options = {
      url: options
    }
  }
  // 获取 Token
  let accessToken = await getToken()

  // 将 Token 设置在 header 中
  let header = optons.header || {}
  header.Authorization = 'Bearer ' + accessToken
  options.header = header

  return request(options, showLoading)
}
```
#### 退出登录
```javascript
const logout = async (params = {}) {
  let accessToken = wepy.getStorageSync('access_token')
  // 调用删除 Token 接口，让 Token 失效
  let logoutResponse = await wepy.request({
    url: host + '/' + 'authorizations/current',
    method: 'DELETE',
    header: {
      'Authorization': 'Bearer ' + accessToken
    }
  })

  // 调用接口成功则清空缓存
  if (logoutResponse.statusCode === 204) {
    wepy.clearStorage()
  }

  return logoutResponse
}
```
别忘了导出
```javascript
export default {
  request,
  authRequest,
  refreshToken,
  login,
  logout
}
```